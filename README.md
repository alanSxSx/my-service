PARA EXECUTAR PRIMEIRAMENTE TEM DE BAIXAR O DOCKER.

Depois de entrar no projeto, você pode fazer a imagem do docker usando o comando:

docker-compose up --build.

Também temos que executar localmente o projeto:

npm run start
